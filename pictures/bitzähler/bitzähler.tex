\documentclass{standalone}
\usepackage{import}
% Import all Packages from Main Preamble with relative Path
\subimport*{../../}{preamble}
% Get Labels from Main Document using the xr-hyper Package
\externaldocument{../../AuD-Zusammenfassung-2020}
% Set Graphics Path, so imported pictures load correctly
\graphicspath{{../../}}


\begin{document}
\begin{tikzpicture}
    \def\blockheight{2em}                                       % Cell height
    \def\blockwidth{3.5em}                                      % Cell width
    \@ifundefined{c@lastelement}{\newcounter{lastelement}}{}    % Storage  for the number of the last element
    \def\contentt{$0$/$b_{k-1}$, 0/$b_{k-2}$, $\cdots$/{}, , , $\cdots$/{}, 0/$b_2$, 0/$b_1$, 0/$b_0$}     % Input of content for the array
    % draw Array
    \foreach [count=\pos from 1] \x/\y in \contentt{
        \node[                                                                      % Node with the cell content 
            minimum height=\blockheight, 
            minimum width=\blockwidth,
            inner sep=0pt, outer sep=0pt,
            ] (n\pos) at (\pos*\blockwidth,0) {\x};
        \node[above = 0mm of n\pos] {\y};                                           % Node with the cell title
        \ifnum\pos > 1 {                                                            % Drawing the inner seperation lines
        \draw[very thick, draw=fgcolor](n\pos.south west)--(n\pos.north west);}     
        \else {} \fi
        \setcounter{lastelement}{\pos}                                              % Saving number of the last element
    }
    \node[fit={(n1.north west) (n9.south east)}, very thick,fill=none, draw=fgcolor, inner sep=0pt, outer sep=0pt]{};   % Drawing the outer frame
\end{tikzpicture}
\end{document}
