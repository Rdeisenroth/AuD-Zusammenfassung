\documentclass{standalone}
\usepackage{import}
% Import all Packages from Main Preamble with relative Path
\subimport*{../../}{preamble}
% Get Labels from Main Document using the xr-hyper Package
\externaldocument{../../AuD-Zusammenfassung-2020}
% Set Graphics Path, so imported pictures load correctly
\graphicspath{{../../}}

\begin{document}
%Styles
\tikzstyle{graphNode}=[align=center, draw=., circle, semithick, text width=6mm, inner sep=0pt]
\begin{tikzpicture}[scale=2, auto]
    %Let's first draw the vertices
    \foreach \nodeNum/\pos in {{1/(0,0)},{2/(1,0.5)},{4/(1,-.5)},{3/(2,0)}}
    \node[graphNode](n\nodeNum) at \pos {$\nodeNum$};

    % Connect vertices with edges and draw weights
    \foreach \source/ \dest /\weight /\options in {n1/n2/1/above, n2/n3/1/above, n1/n4/4/below, n4/n3/-3/below}
    \path[draw, semithick, ., -{Triangle}] (\source) -- node[font=\small, \options] {$\weight$} (\dest);
\end{tikzpicture}
\end{document}
